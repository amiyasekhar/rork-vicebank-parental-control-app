{
  "name": "docs",
  "private": true,
  "scripts": {
    "dev": "bun --hot ./index.html",
    "build": "bun build ./index.html --outdir ./dist --public-path / --minify && mkdir -p dist && cp -R ./public/* ./dist/ || true",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vitejs/plugin-react": "^5.0.2",
    "vite": "^7.1.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
